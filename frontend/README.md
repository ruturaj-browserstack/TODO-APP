# Product Frontend (React + Vite)

## Setup

```bash
npm install
```

## Run

```bash
npm run dev
```

App: http://localhost:5173 (dev server proxies `/api` → `http://localhost:8000`)

## Features

- Login (admin / admin123)
- Todo list (add, toggle complete, delete)
- Counter (increment / decrement / reset)
